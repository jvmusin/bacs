spring:
  profiles:
    include: aggregator-api,standings-updater
  datasource:
    hikari:
      maximum-pool-size: 20
logging:
  level:
    istu.bacs.background.combined: TRACE
---

spring:
  profiles: cloud
  datasource:
    hikari:
      maximum-pool-size: 3