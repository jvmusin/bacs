spring:
  profiles:
    include: aggregator-api,standings-updater
  datasource:
    hikari:
      maximum-pool-size: 20
logging:
  level:
    istu.bacs.background: TRACE
---

spring:
  profiles: cloud
  datasource:
    hikari:
      maximum-pool-size: 3