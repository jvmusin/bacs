server:
  port: 80
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/bacs
    continue-on-error: true
  jpa:
    hibernate:
      use-new-id-generator-mappings: false