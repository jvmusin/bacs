server:
  port: 80
  tomcat:
    accept-count: 10000
spring:
  profiles:
    include: 'db,sybon-api,fake-api'
  datasource:
    hikari:
      maximum-pool-size: 50
---

spring:
  profiles: cloud
  datasource:
    hikari:
      maximum-pool-size: 7